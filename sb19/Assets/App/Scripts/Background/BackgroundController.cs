
using UnityEngine;

public class BackgroundController : MonoBehaviour
{
    /*public UIStatus StatusDisplay;
    public Sprite[] roomBackgrounds;
    public Sprite[] miniGamesBackgrounds;
    public Sprite Gardening;
    [SerializeField] SpriteRenderer backgroundRenderer;

    [SerializeField] float yOffset = 5.0f;
   private int currentRoomIndex = 0;
    public void ChangeBackground(int currentRoomIndex)
    {
        backgroundRenderer.sprite = roomBackgrounds[currentRoomIndex];

        if(currentRoomIndex == 0) StatusDisplay.DimLight.RemoveFromClassList("dim-light");
        else if(currentRoomIndex == 1) StatusDisplay.DimLight.AddToClassList("dim-light");

        Vector3 pos = backgroundRenderer.transform.position;
        pos.y = yOffset;
        backgroundRenderer.transform.position = pos;

        var scaler = backgroundRenderer.GetComponent<BackgroundScaler>();

        if (scaler != null) scaler.ScaleNow();
    }*/
}
